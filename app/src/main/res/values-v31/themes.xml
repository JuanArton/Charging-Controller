<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ChargingCurrentController" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/main_500</item>
        <item name="colorPrimaryVariant">@color/main_dark_800</item>
        <item name="android:windowBackground">@color/accent2_900</item>
        <item name="cardBackgroundColor">@color/neutral1_50</item>

        <item name="android:textColor">@color/black</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.ChargingCurrentController" parent="Base.Theme.ChargingCurrentController" />
</resources>